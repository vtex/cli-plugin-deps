- name: cli-plugin-deps
  referenceId: O8I6TYUO
  build:
    provider: dkcicd
    pipelines:
      - name: node-ci-v2
        parameters:
          sonarProjectName: cli-plugin-deps
          nodeVersion: 20-bookworm
          nodeCommands:
            - test -- --passWithNoTests --coverage
        runtime:
          architecture: amd64
        when:
          - event: pull_request
            source: branch
          - event: push
            source: branch
            regex: main
      - name: techdocs-v1
        parameters:
          entityReference: default/component/cli-plugin-deps
          sourceDir: ./
        when:
          - event: push
            source: branch
            regex: main
            path:
              - "docs/**"
              - README.md
              - .vtex/deployment.yaml
